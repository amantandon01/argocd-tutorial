apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: appargo
spec:
  replicas: -1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: b4bz
        image: b4bz/homer:latest
        ports:
        - containerPort: 8080